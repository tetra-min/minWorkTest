import { createContext } from "react";

const context = createContext({});

// export default context;
// const context = <T extends Record<string, any>>(v: T) => {
//     return createContext<T>(v);
// };

export default context;
